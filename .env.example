# PostgreSQL (required) — Neon https://neon.tech or Vercel Postgres
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Session signing (generate a long random string for production)
JWT_SECRET="replace-with-a-long-random-secret"
